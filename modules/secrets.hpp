#include <vector>
#include <string>
#include <sstream>
#include <iomanip>

#ifndef SECRETS_HPP
#define SECRETS_HPP

std::string SHA256(std::string);
uint32_t rightRotate(uint32_t, uint32_t);

#endif